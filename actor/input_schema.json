
{
  "title": "X402 Domain Scanner Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "domain": {
      "title": "Root Domain",
      "type": "string",
      "description": "Domain to scan, e.g., api.company.com",
      "editor": "textfield",
      "prefill": ""
    },
    "paths": {
      "title": "Paths to Check (Optional)",
      "type": "string",
      "description": "List paths to check, one per line. Leave empty to use the built-in dictionary.",
      "editor": "textarea",
      "prefill": ""
    },
    "maxPaths": {
      "title": "Max Paths",
      "type": "integer",
      "description": "Maximum number of paths to try when using the built-in dictionary",
      "default": 100
    },
    "timeout": {
      "title": "Timeout (ms)",
      "type": "integer",
      "description": "Timeout per request in milliseconds",
      "default": 5000
    },
    "includeSubdomains": {
      "title": "Include Subdomains",
      "type": "boolean",
      "description": "Also scan common subdomains like api.domain",
      "default": false
    }
  },
  "required": ["domain"]
}
